import React, { useEffect, useState } from 'react';

const SiteMetrics = () => {
  const [siteData, setSiteData] = useState({});

  useEffect(() => {
    
  }, []);

  return <div>SiteMetrics</div>;
};

export default SiteMetrics;
